# /test — перевірка без змін коду

1. [[qa]]: запустити наявні тести/lint (`php -l`, `node --check`, тести проєкту).
2. Рендер ключових сторінок (локальний vhost/прод) — HTTP-коди, консоль, обидві мови.
3. Звіт: passed/failed з виводом; фікси — окремо через /fix.
