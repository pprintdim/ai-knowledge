# /feature — нова функціональність

architect → спеціаліст (∥ frontend) → qa → cleanup

1. [[architect]]: декомпозиція, ризики, план, вибір агентів; память+Knowledge перед кодом.
2. Реалізація у worktree (`agent/TASK-ID-name`); незалежні backend/frontend частини — паралельно, спільні файли — ні.
3. [[qa]] по acceptance criteria → [[cleanup]] (push-first, звіт, harvesting у Knowledge).
