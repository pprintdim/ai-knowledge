# /performance — швидкодія

performance → спеціаліст → qa

1. [[performance]]: виміряти (цифри ДО), знайти вузьке місце (EXPLAIN/профіль/network).
2. Спеціаліст: точковий фікс, не «оптимізація всього».
3. [[qa]]: цифри ПІСЛЯ + regression; кеш-інвалідація не зламана ([[common-bugs]]).
