# shokeru — MEMORY

## Структура теки (з 2026-09-08, як hydrophob/)
`/Applications/MAMP/htdocs/shokeru/` — контейнер: ` shoker.in.ua/` (новий OC, репо `pprintdim/shoker.in.ua`), `shokeru.in.ua/` (клон `pprintdim/shokeru` main — код живого сайту, адмінка на проді `shk_panel/`), `shokstore/` (аналіз shokstore.com.ua), `materials/` (спільна сировина, не git).

## materials/ — перепарс донора paralizator.com.ua (2026-09-08)
- Донор має UA-версію `/ua/<slug>/` — спарсено обидві: `parser/data/products.csv` (RU, 150) + `products_ua.csv` (UA, 150), `blog.csv` + `blog/<slug>.{ru,ua}.html` (6 статей), `blog_images/`.
- Старий парсер брав фото з кешу `-1400x1300` (апскейл) → мило на shokeru.in.ua. Тепер `download_images.php` качає справжні оригінали `image/catalog/...` (972 файли, 863 з них 1024×1024) у `parser/data/images_original/`.
- Вотермарк «paralizator.com.ua»: контурний напівпрозорий текст; сила/розмиття різні на кожному фото → математичний розблендинг (Dekel) НЕ зійшовся, відкинуто. Геометрія: масштаб 1.0 (центр, bbox x 88–944 y 473–554) або 1.08 (фото апскейлені з менших), на 1080px-фото — 1.24 на всю ширину (pos 2,485); завжди по центру вертикально.
- Робочий пайплайн (usе в `parser/tools/`): `unwatermark.py estimate` → медіана градієнтів по 250 фото → `data/wm-model.npz` (edges) + `data/wm-mask-9.png` (літери, dilate 9; вужча маска лишає «привидів»); `wm_locate.py` — пошук масштабу з фіксованим центром → `data/wm-locate.csv` (corr: без вотермарку ≤0.1, з — 0.16+; поріг 0.11); `locate_big.py` — 2D-пошук для масштабу ≥1.16 (може виходити за край); `lama_prep.py` — індивідуальна маска на кожне фото (+ширша dilate 15 там, де фон білий, інакше на білому лишається сяйво) → `iopaint run --model=lama --device=mps` (venv `tools/venv`, python3.12, ~0.7 фото/с на M4; для тонких предметів на білому — `--config` з `{"hd_strategy":"Original"}`, інакше LaMa заливає предмет білим) → `lama_collect.py` → `make_review.py` → `materials/review/index.html` (дивитись через MAMP `http://localhost:8888/shokeru/materials/review/`).
- Результат 2026-09-08: 972 фото в `materials/images-clean/` (808 через LaMa, 164 без вотермарку як є). Вибірково перевірено; на етикетках з дрібним текстом під вотермарком LaMa домальовує (нечитабельно, але на картці непомітно).
- Блог: вотермарк лише на 10 фото `blog/luchshie/luchshiy-shoker-*.jpg` (800×~340, ті самі товари) — замінити чистими каталожними.
- 2026-09-09: фото залито на прод, каталог синхронізовано (див. розділ нижче).

## Прод shokeru.in.ua (2026-09-09)
- Хостинг: Hetzner 46.224.100.254 (CloudPanel), site user `shokeru`, корінь **`/home/shokeru/htdocs/shokeru.in.ua`** (є дубль `/home/pprintdim/htdocs/shokeru.in.ua` — не живий, nginx дивиться на shokeru). Адмінка `/shk_panel/`. БД `shokeru`, креди — `app_config.local.php` у корені сайту. PHP CLI: `php8.2`. Старий FTP m366/monosite мертвий.
- SSH/SCP: `~/AI-Workspace/scripts/hetzner-ssh.sh '<cmd>'` / `--scp <file> <remote>` — той самий Keychain-механізм, що й `hetzner-mysql-tunnel.sh`; пароль у вивід не потрапляє.
- Залито чисті фото (`materials/images-clean` → `image/catalog/{fototovarov,uzotpugivateli}`), кеш `image/cache/catalog/*` і `system/storage/cache` скинуто; галерея товару піднята до 1024 (`theme_default_image_popup_*`, `theme_default_image_thumb_*` = 1024 — сайт користується кодом `theme_default`, webp q90). Лістинг лишився 500×500.
- `sync_catalog.php --apply`: 17 цін, 3 увімкнено, 14 вимкнено (нема в донора), 12 uk-назв. Бекапи локально: `shokeru.in.ua/backups/backup-db-20260909101716.sql.gz`, `backup-images-20260909101716.tar.gz`, `sync-backup-20260909.json`.
- Не чіпалось: `image/catalog/cutout/*.webp` (5 старих вирізок для блоків головної), описи (SEO-згенеровані), блог.


## Контент донора і добірка каталогу (2026-09-11)
- Донор `paralizator.com.ua` має більше, ніж брав старий парсер: **UA-версію** (`/ua/<slug>/`), 19 SEO-лендінгів (міста/країни/призначення), 7 інфо-сторінок, 562 відгуки (в картках товарів + розділ `elektroshoker-otzyvy`), 11 категорій з великими SEO-текстами. Розділ `elektroshoker-video/` ПОРОЖНІЙ — відео в донора нема.
- Зібрано в `materials/`: `content/{categories,seo-landings,pages,reviews,products,blog,contacts}.md`, `seo/{product,category,page}-seo-meta.csv`, `parser/data/{pages.csv,pages/,reviews.csv,contacts.json,filters.csv,filter_groups.csv,page_images/}`. Збір — `parse_content.php --pages|--reviews|--contacts`, зведення — `tools/build_content_md.php`, фільтри — `tools/export_filters.php`.
- **Пастка розмітки донора**: текст сторінки не завжди в `#content` — трапляються `.service-landing`, `.art-bl`; чистий SEO-текст категорій/лендінгів лежить у `#description` (зберігається окремо як `<slug>.seo.<lang>.html`), у загальному контейнері до нього домішується сортування й картки товарів.
- **Каталог**: донор ховає з лістингу 14 товарів (сторінки живі) — вони в `product_urls.csv` зі статусом `hidden`; знайдено 3 товари, яких у нас не було (відлякувачі D16, PU-11, CW001). Разом 167 товарів, 990 фото (16 нових пройшли той самий LaMa-пайплайн).
- **Аудит прода проти hydrophob.net** (2026-09-11): нема відгуків (`oc_review`=0), немає заявок `oc_lead` («купити в 1 клік»), немає `oc_seo_meta`, немає аналітики (жодного `extension` типу analytics) і фіда `feed/google_base`, `config_compression=0`, пошта на `smtp` з `config_email=pprintdim@gmail.com` (правило проєктів — `owner@<домен>` через Brevo HTTP API), 10 з 11 категорій були без meta_description, з інфо-сторінок були лише 3 (політика, умови, оплата/доставка).
- **Пастка SSH**: при частих підключеннях сервер починає рвати сесії (`Connection closed by ...`) — робити менше заходів, пакувати все в один скрипт, після обриву чекати кілька хвилин.
- **Застосовано на прод 2026-09-11**: +3 товари (167, активних 70; CW001 вимкнений бо «нема в наявності» — його сторінка 404, як і решта вимкнених), meta категорій 30/30 рядків власними шаблонами Shokeru, 2 інфо-сторінки з мовними слагами (`garantiya-ta-povernennya`/`garantiya-i-vozvrat`/`warranty-returns`, `servis-i-remont`/`remont-elektroshokerov`/`service-repair`) — обидві в футері й у `sitemap/pages.xml`. «Про нас» не додавав: слаг `about-us` уже зайнятий route-сторінкою теми. Бекап БД перед змінами — `shokeru.in.ua/backups/backup-db-20260911172703.sql.gz`.
- Інструменти застосування (усі з dry-run за замовчуванням, `--apply` для запису): `tools/{sync_catalog,add_missing_products,fill_category_meta,add_info_pages}.php --oc=<корінь>`; `gen_seo_descriptions.php --only-sku=...` генерує RU-опис у стилі каталогу для окремих товарів (робить `.pre-gen.bak`).
