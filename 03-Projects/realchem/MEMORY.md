# realchem — MEMORY

## 2026-09-21 (ніч) — welcome-накладка + кошик (коміти 7d12c24, 89b6437 / main ffeae48)
- **Welcome**: окремої сторінки більше немає. lovable коміт `5aa10b7` видалив `routes/welcome.tsx` і додав `components/home/IntroOverlay.tsx`; hydrophob.net.ua зробив те саме («лишилась сама накладка», `home.php::welcomeOverlay()` + `welcome_overlay.twig`). realChem тепер так само: `common/welcome` прибрано (404), `home.php` кладе накладку в `{{ welcome_overlay }}`, вигляд з IntroOverlay, частота — RCWelcome (кука `rc_welcome` + адмінка). **Без відеофайлу накладки нема і показ не списується в куку.** В адмінці Дизайн → Вітальний тизер зʼявились поля відео/постера (filemanager приймає лише зображення → відео вводиться шляхом; якщо треба пікер — розширити whitelist у `rc_panel/controller/common/filemanager.php`).
- **Кошик**: ЧПУ `/cart` і `/checkout` (слаги в oc_seo_url uk/ru); крихти Головна//Магазин//Кошик; колонки «Товар/Ціна/Кількість/Сума»; рядок «Доставка — за тарифами», проміжні підсумки лише коли інформативні; мініатюра 47×47 → 128×128 (`theme_realchem_image_cart_*`).
- **Граблі**: `html.realchem.com.ua` віддає СТАРУ верстку (без cart.php тощо) — для звірки брати `git show origin/main:<файл>`, не сабдомен. Headless Chrome залипає, якщо лишились процеси з минулих запусків: `pkill -9 -f -- "--headless"` + `--user-data-dir=/tmp/rc_chrome_profile`.
## 2026-09-21 (вечір) — бекенд за стандартом §11 (коміт d0f6d91, задеплоєно)
- rc_panel: hydro-repeater/tabs/langs/media (+css) з hp_panel; 7 форм home_* — таби Основні/Контент/Показ, один перемикач мов (лише активні, uk перша), рядки згорнуті/drag, медіа-плитка (шлях нормалізується в image/… у `RealchemAdminModule::mediaPath()`), підписи/hint-и в мовних файлах (`field_*`, `help_*`, `form_groups`). `data-field` замість `data-prefix` у hydro-repeater.js (контракт content[lang][field][i][key]). Twig адмінки = ArrayLoader → 7 копій twig з одного джерела.
- Нові: `catalog/blog` (адмінка блогу; body = JSON-масив абзаців → повторювач; ЧПУ blog_post_id= по мовах), `design/welcome` (welcome_* в oc_setting, RCWelcome::apply($config)), drag&drop у design/layout. Меню: без Маркетплейсу, + Блог, + Тизер. Права групі 1 додано SQL; oc_language sort_order uk1/ru2/en3.
- Адмін-логін для перевірок: POST common/login → 302 з user_token (креди в ACCESS.md), далі curl з кукі; скріншоти — зберегти HTML, замінити `view/` на абсолютний `rc_panel/view/`.
- hydrophob/* репо мають чужі незакомічені зміни (паралельна сесія користувача) — наші агенти туди не писали.
## 2026-09-21 — OTP + кабінет зроблено
- Коміти: openCart `25b47a8` (OTP, кабінет-сайдбар, 302 стокових, 13 twig видалено), `41beb3d` (реальні відгуки замість demo + форма), `4319b4a` (іконки картки, play, z-[80]); main `bb56a51` (auth-modal кодом + code-modal). Обидва задеплоєні (realchem.com.ua, html.realchem.com.ua). БД: клієнтів 0, відгуків 0 (тестові прибрано).
- OTP: `common/user_popup` (код у `oc_session` — сесії в БД, `session_engine=db`), `common/code_modal`, `window.rcOtp.start(ctx)`. Перевірено наскрізно curl-ом (код читав з oc_session.data).
- Кабінет: `account/nav` — сайдбар через `$this->load->controller('account/nav', array('active'=>…))`; сторінки account (профіль, email readonly + «Змінити email» → rcOtp type=email), order (фільтри за status_class), wishlist (картки, data-cart-add). Лишились twig: account, edit, nav, order_list, order_info, wishlist.
- Ще зроблено: валюта UAH → `₴` префікс; config_meta_title/description заповнено; text_account у крихтах = «Кабінет» як у верстці.
- Відкрите (пріоритет): чекаут стоковий (§6 — односторінковий, НП, wayforpay); мінікошик-шухляда; [data-wishlist-add] на картках без обробника (треба в index.js: POST account/wishlist/add, гість → auth); intl-tel-input/normalizePhone; фільтри каталогу; sitemap/robots; адмін-форми модулів §11; адмінка блогу; config_telephone=123456789 і hardcode телефону в шапці — дані від клієнта.
## 2026-09-20 — аудит паритету + стандарт групи, рішення
- **Рішення користувача**: авторизація — OTP-код як у групі hydrophob (не пароль з верстки); кабінет — сайдбар-макет верстки (Профіль / Замовлення / Обране / Вийти), усі стокові `account/*` → 302 у кабінет. Верстка `main` оновлюється дзеркально (auth-modal → код).
- Посадка 20 стокових account/*-сторінок 19–20.09 (`b0fe57d…3a35d9f`) виявилась роботою «в мінус» проти стандарту — списується при перебудові кабінету. Лишаються `account`, `order_list`/`order_info`, `wishlist`, `edit`, новий `nav`.
- Виконавці: Codex — квота OpenAI вичерпана (10:59 PM); Fable — 429 до 16:00 Tirane. OTP і кабінет → два Sonnet-субагенти паралельно (брифи в scratchpad: `codex-brief-otp.md`, `brief-cabinet.md`; контракт між ними `window.rcOtp.start(ctx)`).
- **Полагоджено сьогодні** (усе на проді, коміти `754bac0…b8cdcf1`): навігація вела на стокові сторінки (`links()` у `catalog/model/extension/module/realchem.php` → тепер `information/about|contacts|delivery|blog`, `product/category`); вихідний rewrite слагів роутів у `seo_url.php` + слаги `/shop /contacts /blog` (`/catalog` не можна — тека ядра, nginx 403); `[data-cart-add]` був демо-заглушкою + `opencart.js` capture-перехоплював (кнопки «в кошик» на головній/каталозі не працювали, іконка кабінету → `/null`); `text_home` у базових uk/ru/en-gb = текст; `config_country_id` 222→220, zone→3490 (Київ); 40+ Tailwind-утиліт додано в `custom.css` (скрипт-чекер `scratchpad/check-classes.php`, звіряє кожен `class=` з CSS; перевіряти кожен новий twig).
- **Відкрите за аудитом** (не зроблено): чекаут стоковий Bootstrap (потрібен порт односторінкового з hydrophob.ua §6 + НП + wayforpay); мінікошик-шухляда; demo_reviews у product.twig (фейкові відгуки на лайві!); фільтри каталогу статичні; sitemap 404, robots без User-agent; `config_meta_description`='My Store', `config_telephone`=123456789 (дані від клієнта); формат ціни `1 180,00 грн.` vs верстка `₴1 180,00` (oc_currency UAH symbol); адмін-форми модулів без hydro-repeater/tabs (§11); адмінки блогу нема; топ-продаж/instagram — карусель (з lovable-diff, main застарів — не баг).
- `page-auditor`/`standard`-звіти — у транскрипті сесії f7b5b0b8; головне перенесено сюди.

## 2026-09-15 — нульовий етап
- Див. auto-memory `realchem-project-setup.md` (гілки, БД лише на проді, rc_panel, креди в ACCESS.md).

## 2026-09-09 — html-сабдомен верстки знято
- Верстка = гілка `main` репо pprintdim/realchem, worktree `site/html/` прибрано; 2026-09-15 `html.realchem.com.ua` повернуто (DNS, CloudPanel, LE, деплой з `main`).
