# stocrm — TASKS

## Активні (з повної специфікації, ще не зроблено)

- [ ] Комплексний аудит сідерів на всі 15 демо-сценаріїв зі специфікації
- [ ] Розширити test suite: Client/Vehicle/Appointment/WorkOrder CRUD, InventoryService/WorkOrderCalculatorService формули; нові фічі 08-23/24 (bulk delete нарядів, update діагностики/рекомендацій, login-авторизація) тестами не вкриті
- [ ] Фінальний продакшн README.md (seeders/demo credentials/бізнес-правила) — поточний README оновлено лише назвою бренду, не переписаний по суті
- [ ] Підключити фронтенд до `POST /appointments/{id}/convert` — кнопка «Створити наряд» на Календарі зараз не переносить дані заявки (див. MEMORY.md)
- [ ] Calendar.tsx досі на mock-даних (`@/mocks/appointments`) — тижнева сітка показує НЕ реальні заявки; кнопка «Підтвердити запис» у Sheet без обробника
- [ ] PDF з «Зберегти PDF» — растровий (html2canvas-pro); якщо потрібен текстовий PDF — серверна генерація
- [ ] Позашляховиковий розділ прайсу (нормо-години відрізняються) не заведений у довідник — на фото лише №1–34; чекати повних фото від користувача

## Завершені (2026-08-23/24, сесія «наряди/мобілка/логіни»)

- [x] Наряд: інлайн-редагування рядків усіх вкладок + bulk delete; редагування самого наряду (модалка, все крім клієнта/авто, дата заднім числом); клік по «Загальних даних» відкриває модалку, редаговані значення підсвічені синім
- [x] /work-orders: bulk delete (owner, зі звільненням резервів), олівець+кошик у рядках
- [x] Уніфіковані RowActions (олівець/кошик + ConfirmDialog) на всіх списках
- [x] Тип робіт: −Гарантія, +Кузовні/ДВЗ/Ходова (спільний lib/workOrderOptions)
- [x] Години роботи опціональні (прочерк «—» у таблицях і друку)
- [x] Друк: фікс мертвих кнопок (pointer-events лок від Radix + oklch у html2canvas) → нова вкладка, html2canvas-pro+jsPDF «Зберегти PDF»
- [x] Довідник робіт: 139 позицій з фото прайсу (PriceListWorksSeeder, 700 грн/год, описи перенесені)
- [x] Мобільний адаптив: DataTable → картки, скрольні таби, одноколонкові форми в модалках, «Розрахунок» першим на телефоні
- [x] «Запчастини» злиті в «Склад» вкладками (Рухи/Позиції), пункт меню прибраний
- [x] Авторизація по коротких логінах (email → <login>@autovibe.local під капотом; legacy повні email працюють); email прибраний з працівників і клієнтів
- [x] SFTP (.vscode/sftp.json) налаштований; materials/ виключена з git/deploy/sftp

## Завершені (2026-08-07, ця сесія)

- [x] Лого AutoVibe: обрізаний прозорий PNG (`public/logo.png`), favicon.ico + 16/32/apple-touch/192/512, підключено в `app.blade.php` + сайдбар + логін
- [x] Ребрендинг Mechora → AutoVibe по всій видимій UI (email-домен демо-логінів навмисно лишився `@mechora.ua`)
- [x] Instructions.tsx повністю переписано: per-role зміст з якорними лінками, покрокові інструкції (steps) з deep-link на кожен розділ адмінки
- [x] Dashboard bug-фікс: картка «Активні наряди» тепер веде на відфільтрований `/work-orders?status=active`, а не на повний список (`WorkOrderStatus::active()`, `status=active` у контролері й фільтрі)
- [x] Задеплоєно на `sto.pprintdim.com`, закомічено в git (`5957260`)
